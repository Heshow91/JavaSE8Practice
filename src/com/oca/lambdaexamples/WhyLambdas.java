package com.oca.lambdaexamples;

public class WhyLambdas {

//	Enables functional programming
//	Readable and concise code
//	Easier to use APIs and libraries
//	Enables support for parallel processing
//	instead of creating a class that implements an action
//	we can have an action being passed in instead = lambda functions
	
//	InLine value String name = "food"; piece of code gets assigned a value
// 	InLine code does not need the modifiers remove them!! and does not need the return type
	
//	Doesn't return anything java is smart to say what the function is returning
	
//	aBlockOfCode =  () -> System.out.println("Hey!");
		
// Ways to write functions
// greetingFunction = () -> System.out.print("Hello World");
// doubleNumberFunction = (int a) -> a * 2;
// addFunction = (int a, int b) -> a + b;
	   
//		Because it is more than one line the lambda function now needs to have the curly braces to separate
//	safeDivideFunction = (int a , int b) -> {
//		if (b == 0) return  0;
//		return a / b; 
	
// stringLengthCountFunction = (String s) -> s.length();
	
}
